@model IEnumerable<ELCAPITAL.Models.FormularioRaiz>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaAprobacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Solicitud)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaAprobacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Solicitud.TipoSolicitud)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFormularioRaiz">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFormularioRaiz">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFormularioRaiz">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
